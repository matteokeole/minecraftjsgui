import * as Component from "./components/index.js";

export default Component;

/**
 * @todo Better GUI module structure
 * No default export.
 * Inspired by React Bootstrap module structure.
 * 
 * 
 * 
 * import * as GUI from "gui";
 * 
 * const layer = new GUI.Layer({...});
 * const image = new GUI.Image({...});
 * 
 * 
 * 
 * import {Layer, Image} from "gui";
 * 
 * const layer = new Layer({...});
 * const image = new Image({...});
 */