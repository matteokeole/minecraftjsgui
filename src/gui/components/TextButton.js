import Component from "./Component.js";

const MAX_WIDTH = 200;

/**
 * A generic button with a text line on it.
 * 
 * @constructor
 * @extends Component
 * @param {{
 *    width: Number
 * }}
 */
export default function TextButton({width}) {
	Component.apply(this, arguments);

	//
}